package com.teamolympus.dangerzone.api;

/*
    An API for AdventureKraft, intended for mods to interface with AdventureKraft.
    Be sure to Isolate the calls properly. Ensure this is only called if the
    AdventureKraft mod is installed. If not, it can cause a NoClassDefFoundError
    crash. If you see a need a redistribute or shade the api files, you have likely
    done something very wrong.
 */
